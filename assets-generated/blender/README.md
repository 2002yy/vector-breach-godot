# Blender generated staging

Use this directory for reproducible Blender intermediates before explicit promotion into the runtime `assets/` tree. Generated files are ignored by Git; this README only documents the namespace.
